(function() {
    /* global angular */
    ponastaviCtrl.$inject = ['$location', 'authentication', '$scope','$route','$window','$http'];
    function ponastaviCtrl($location, authentication, $scope, $route, $window, $http) {
        
    }
    
    angular
        .module('tpo')
        .controller('ponastaviCtrl', ponastaviCtrl);
})();